use crate::types::Parser;

pub struct BasicString;

// impl Parser<String> for BasicString {
//     fn parse<R: std::io::Read>(reader: &mut R) -> Result<String, crate::errors::ParserError> {
//         let mut buf = super::common::read_line(reader);

//         let line = String::f
//     }
// }